setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile

# release:
#   command:
#     - ./deployment-tasks.sh
#   image: worker
  
# run: docker-compose docker-compose.prod.yml up --build
